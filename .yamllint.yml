---
extends: default

rules:
  line-length:
    max: 250
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
  document-start: disable
  braces:
    min-spaces-inside: 0
    max-spaces-inside: 0
  brackets:
    min-spaces-inside: 0
    max-spaces-inside: 0

ignore:
  - .git/
  - cassandra/
  - cassandra-dse/
  - ldap/
